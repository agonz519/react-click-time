import React from "react";
import "../styles/Header.css";

const Header = () => (
  <header className="header">
    <h1>CLICK TIME</h1>
    <h4>...like bullet time, except not at all, well except maybe that you have limited time</h4>
  </header>
);

export default Header;
